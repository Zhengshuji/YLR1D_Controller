# YLR1D 机器人控制系统

# 1 框架结构

分为五层：

1. 感知层：下位机获取传感器信息、预处理，上位机实现高级感知功能（状态估计、SLAM等等）

2. 决策层：上位机基于状态与任务，输出任务目标

3. 规划层：上位机，输出控制关键帧，下位机，逆运动学求解等，实现关节空间精插补轨迹

4. 控制层：下位机实现多闭环控制

5. 硬件层：仿真时配置gazebo参数，实际中调用硬件接口

通信层，需要缓冲、检错。

# 2 建模

## 2\.1 小车底座模型

内部参数：

1. 左右轮间距 d = 0.320 m
2. 前后轮间距 l = 0.426 m
3. 车轮转向角度限制：±3.14 rad (±180°)，速度限制：100.0 unit/s
4. 车轮转速无限制（连续旋转关节）

四个轮子位置（在 Base 坐标系下）为 (±l/2, ±d/2)：

| 轮子 | x (m) | y (m) | 初始转向角 α (rad) | α (deg) |
|------|-------|-------|----|----|
| RF（右前） | -0.213 | +0.160 | +2.497 | +143.1° |
| LF（左前） | -0.213 | -0.160 | -2.497 | -143.1° |
| RB（右后） | +0.213 | +0.160 | +0.644 | +36.9° |
| LB（左后） | +0.213 | -0.160 | -0.644 | -36.9° |

转向机制：四个转向关节（Joint_Base_to_*WheelF）均为绕 Z 轴的 revolute 关节，轴方向同为 (0,0,1)，限制相同。区别仅在于初始角度 α_i。转向角正方向为绕 Z 逆时针。

车轮驱动关节（Joint_*WheelF_to_*Wheel）：Continuous 无限制旋转。左右侧驱动轴方向相反（右侧 (0,0,-1)、左侧 (0,0,1)），使得同一正速度指令下两侧均产生向前的驱动力矩。

外部参数：

1. 合外力（尤其机械臂传导下来的，并且包含重力）G，估计得到

2. 质心位置 P，估计得到

3. 轮胎与地面摩擦力，自适应控制

假设机器人始终没有倒地，则以机器人上固定的平面坐标系Base，位于四个轮子中心，创建四个轮子依次为：（±l/2,±d/2），质心位置在四个轮子内的平面下记为P'。

运动学：

> 轮子接地点实际速度：$V_{space,i} = [V_x - ω * y_i, V_y + ω * x_i]^T $
> 
> 纯滚动无侧滑：纯滚动线速度：$v_i = (V_x - ω*y_i) * \cos\alpha_i + (V_y + ω*x_i) * \sin\alpha_i$
> 
> 设$A=\begin{bmatrix}
> cos\alpha_1& sin\alpha_1 & x1*sin\alpha_1 - y1*cos\alpha_1\\
> cos\alpha_2& sin\alpha_2& x2*sin\alpha_2 - y2*cos\alpha_2\\
> cos\alpha_3& sin\alpha_3& x3*sin\alpha_3 - y3*cos\alpha_3\\
> cos\alpha_4& sin\alpha_4& x4*sin\alpha_4 - y4*cos\alpha_4
> \end{bmatrix}$，
> 
> $X=\begin{bmatrix}
> V_x\\
> V_y\\
> \omega
> \end{bmatrix}$，$b=\begin{bmatrix}
> v_1\\
> v_2\\
> v_3\\
> v_4\\
> \end{bmatrix}$，
> 
> 由$AX=b$，$X=(A^TA)^{-1}A^Tb$
> 
> 

特别地，在SDK中仅有几种特定的运动模式：

1. 无转动运动，所有轮子朝向一致；

2. 原地转动，所有轮子速度方向指向中心

3. 锁死模式

## 2\.2 机械臂模型

内部参数：

1. 各个关节改进DH参数

2. 各个关节角度限制

3. 各个关节速度限制

外部参数：

1. 当前速度（尤其底座传导上来的），估计得到

2. 当前整体加速度（尤其底座传导上来的），估计得到

3. 外力，自适应控制

躯干链：

|i|α\<sub\>i\-1\</sub\> \(rad\)|a\<sub\>i\-1\</sub\> \(m\)|d\<sub\>i\</sub\> \(m\)|θ\<sub\>i\</sub\> \(rad\)|说明|
|---|---|---|---|---|---|
|1|0|\-0\.113|0\.2 \+ q\<sub\>1\</sub\>|0|棱柱关节 \(Base→Body1\)，变量为 d|
|2|0|0|0\.6133|q\<sub\>2\</sub\>|旋转 \(Body1→Body2\)，Z平移已合并原 j3/j4 的 d|
|3|π/2|\-0\.01|0|q\<sub\>3\</sub\>|旋转 \(Body2→Body3\)，α=π/2 使 d 不贡献 Z 平移|
|4|0|0|0|q\<sub\>4\</sub\>|旋转 \(Body3→Body4\)|

左臂链：

|i|α\<sub\>i\-1\</sub\> \(rad\)|a\<sub\>i\-1\</sub\> \(m\)|d\<sub\>i\</sub\> \(m\)|θ\<sub\>i\</sub\> \(rad\)|对应关节（URDF 名称）|
|---|---|---|---|---|---|
|1|π/2|0|\-0\.1338|π \+ q\<sub\>1\</sub\>|Joint\_Body2\_to\_LeftArm1|
|2|π/2|0|\-0\.175|q\<sub\>2\</sub\>|Joint\_LeftArm1\_to\_LeftArm2|
|3|π/2|0|0|\-π/2 \+ q\<sub\>3\</sub\>|Joint\_LeftArm2\_to\_LeftArm3（轴为 Y）|
|4|π/2|0\.246|0\.015|π \+ q\<sub\>4\</sub\>|Joint\_LeftArm3\_to\_LeftArm4（吸收 x=0\.246 为 a）|
|5|π/2|\-0\.015|0|q\<sub\>5\</sub\>|Joint\_LeftArm4\_to\_LeftArm5|
|6|π/2|0|\-0\.265|q\<sub\>6\</sub\>|Joint\_LeftArm5\_to\_LeftArm6|
|7|0|0|0|π \+ q\<sub\>7\</sub\>|Joint\_LeftArm6\_to\_LeftArm7|

右臂链：

|i|α\<sub\>i\-1\</sub\> \(rad\)|a\<sub\>i\-1\</sub\> \(m\)|d\<sub\>i\</sub\> \(m\)|θ\<sub\>i\</sub\> \(rad\)|对应关节（URDF 名称）|
|---|---|---|---|---|---|
|1|\-π/2|0|0\.1338|\-π \+ q\<sub\>1\</sub\>|Joint\_Body2\_RightArm1|
|2|π/2|0|\-0\.175|q\<sub\>2\</sub\>|Joint\_RightArm1\_to\_RightArm2|
|3|π/2|0|0|\-π/2 \+ q\<sub\>3\</sub\>|Joint\_RightArm2\_to\_RightArm3|
|4|π/2|0\.246|0\.015|\-π \+ q\<sub\>4\</sub\>|Joint\_RightArm3\_to\_RightArm4（吸收 x=0\.246 为 a）|
|5|π/2|\-0\.015|0|q\<sub\>5\</sub\>|Joint\_RightArm4\_to\_RightArm5|
|6|π/2|0|\-0\.265|q\<sub\>6\</sub\>|Joint\_RightArm5\_to\_RightArm6|
|7|0|0|0|q\<sub\>7\</sub\>|Joint\_RightArm6\_to\_RightArm7|

直接采用改进DH参数的建模方法即可：

$T^{i−1}_i=\begin{bmatrix}
\cosθ_i & -\sinθ_i & 0 & a_{i-1}\\
sinθ_icosα_{i−1} & cosθ_icosα_{i−1} & −sinα_{i−1} & -d_i\sinα_{i−1} \\ sinθisinα_{i−1} & cosθisinα_{i−1} & cosα_{i−1} & d_icosα_{i−1}\\
0 & 0 & 0 & 1\end{bmatrix}$

特别地，SDK中仅给出直接设置关节广义坐标的正运动学方法、设置末端位置的逆运动学方法（并不非常靠谱）。

## 2\.3 机械夹爪模型

内部参数：

1. 左爪开行程：-0.014；关行程：0

2. 右爪开行程：0；关行程：0.014

Finger1 与 Finger2 的 rpy 绕 Z 相差约 π，因此正 q 值使两指沿相反方向平移（张开），负 q 值使两指相向运动（闭合）。

外部参数：

1. 外力作用，估计得到

## 2\.4 综合建模

首先，明确这些要点：

1. SDK包仅给出基于运动学的控制。因此在设计控制器的时候需要以前馈控制为主，反馈控制为辅；

2. SDK包基本上各部分是分离的，没有将机器人系统作为整体进行考虑。因此，在设计控制器的时候，需要重点关注不同部位之间的配合关系；

3. SDK包内包含的传感器相对受限，因此最好有状态估计器。


# 3 仿真控制系统设计

由于小车底座SDK没有提供太多信息，因此暂时这样写。

## 3\.1 物理层

模拟实现真实机器人效果，主要包含两个方面的内容：

1. 运动学仿真，控制位置。并且这种方法更加契合SDK包。

2. 动力学仿真，控制力、力矩。这个更加真实，但SDK包已经将这部分内容涵盖。

这里采用了xacro模型，便于调整参数。

此外，为了提升仿真效率，需要尽量降低gazebo的开销。（一方面，不需要gazebo界面可视，另一方面，调整gazebo仿真设置）

并且，需要考虑机器人自身部件碰撞产生的影响。（待定）

> 原本模型中：
> 
> 1. 左侧轮，Joint\_LFWheelF\_to\_LFWheel、Joint\_LBWheelF\_to\_LBWheel速度为正时反转，需要调整
> 
> 2. 左臂，Joint\_Body2\_to\_LeftArm1、Joint\_LeftArm2\_to\_LeftArm3、Joint\_LeftArm3\_to\_LeftArm4、Joint\_LeftArm5\_to\_LeftArm6、Joint\_LeftArm6\_to\_LeftArm7，这些关节反向
> 
> 3. 右臂，Joint\_RightArm2\_to\_RightArm3、Joint\_RightArm3\_to\_RightArm4、Joint\_RightArm5\_to\_RightArm6、Joint\_RightArm6\_to\_RightArm7，这些关节反向
> 
> 4. 夹爪有问题，但受初始位置限制，后面再说。
> 
> 另外，SolidWorks导出的urdf，无法更改之前错误的结果。
> 
> 

## 3\.2 控制层

运动控制指令封装、发送。

输出方，考虑与SDK包适配的需求，保证输出与SDK包一致，即将机器人系统拆分为躯干、左臂、右臂、底座四个独立部分，各部分整体控制。

输入方，考虑在机器人系统决策规划是以整体进行，若输入为整体，则需要拆分重组，同时每个控制量应该能够保持。因此，输入是以一个话题、不同配置，实现每个关节的控制。

内部主要包含：

1. 实现闭环PID控制器。**（实际上，可以扩展为任意离散系统模型，这里以PID控制器为例）**

    1. 对于位置控制，直接假设输出量为加速度，系统模型为$G(s)=\frac{1}{s^2}$

    2. 对于速度控制，假设输出量为加速度，系统模型为$G(s)=\frac{1}{s}$

2. 输入、输出以及中间状态变量限幅处理操作

3. 适当对输入量进行调整，以弥补建模过程中产生的问题

    1. 夹爪，使用0、1控制开、合即可。并且，两个夹指只需要任意控制其中一个就行（还是迁移到规划层）

    2. ~~部分关节，由于建模问题，导致Z轴方向恰好相反~~（直接在模型中修改）

---

## 3\.3 规划层

分为两个部分：小车底座、机械臂移动

并且，从这层起，主要采用Action的方式进行通信。

### 3\.3\.1 小车底座

封装固定的移动模式，接受速度、方向输入，解算得到对应控制量。并且，总假设轮子无滑滚动。

1. 平移方式：所有轮子保持一致朝向，速度保持一致。

    > 轮子速度与给定一致，轮子朝向与方向一致。特别地，有这样两种方式：
    > 
    > 

    1. X方向平移模式（仿真省略）

    2. Y方向平移模式（仿真省略）

2. 原地旋转方式：所有轮子速度方向交于旋转中心（四个轮子形成矩形的重心）

    > 方向固定$\begin{bmatrix}2.2150\\
    >     0.9265\\
    >    -2.2150\\
    >    -0.9265\end{bmatrix}$，速度正负取决于转动方向，大小取决于转动速度。并且，有$\omega = 3.7538*v$
    > 
    > 

3. 停车模式：所有车轮轮速为0

    1. 两对角轮子方向平行

    > 方向固定$\begin{bmatrix}   -0.6443\\
    >     0.6443\\
    >    -2.4973\\
    >     2.4973\end{bmatrix}$，速度为0。
    > 
    > 

另外，总是先完成转向，再进行移动。

~~接收超声波传感器信息并处理（传感器后续确认再说）~~

### 3\.3\.2 机械臂

按照决策层的规划，平滑、实时避障等操作，生成可执行的关节空间序列。

1. 平滑：

2. 避障（待定）

3. 动态响应（待定）

4. 采用BOOL量控制夹紧、松开。



## 3\.4 决策层

### 3\.4\.1 小车底座

按照SLAM等，进行运动决策

### 3\.4\.2 机械臂

末端轨迹规划，关节空间解算

### 3\.4\.3 融合决策

小车底座、机械臂怎么联动

躯干、双臂怎么联动



## 3\.5 感知层

主要用于处理不同传感器，功能相对分散。有时还需要必要的标定算法。

### 3\.5\.1 机械臂关节速度、加速度、力估计

主要依靠关节位置信息。

### 3\.5\.2 机器人系统位置、速度、加速度

IMU、雷达、视觉等，可能采用超声波传感器

SLAM，位置估计

### 3\.6\.3 图像处理

不同功能、不同部位的相机。

# 4 人机交互界面

## 4\.1 控制层

基于控制层，该人机交互界面的设计，基本与SDK包提供的功能一致，主要包含这些要点：

1. 展示当前各个关节状态

    1. 展示机器人的关节组成、分组情况**（有些内容没有介绍到）**

    2. 展示各个关节角度、速度**（物理量没有明确。并且需不需要单位转换？）**

2. 实现对各个关节的控制**（仅包含SDK的关节空间控制）**

3. rviz2内嵌，展示机器人模型（待定）

## 4\.2 规划层

支持以封装的指令，直接控制小车底座、机械臂。

1. 小车底座：

    1. 移动方式 \-\> 车轮朝向

    2. 移动速度 \-\> 车轮速度

2. 机械臂：

    1. 机械臂编号 \-\> 关节名称组

    2. 机械臂各个关节角度 \-\> 广义坐标向量

    3. 机械臂速度限制 \-\> 具体控制参数

    4. 机械臂夹爪开合 \-\> 直接控制机械夹爪位置





