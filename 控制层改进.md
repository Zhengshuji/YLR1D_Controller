# 控制层改进

## 0. 背景与问题

### 0.1 阶段 A 已解决的问题（2026-08-06 完成）

现状 `ylr1d_control_sim` 最初存在结构性问题，阶段 A 已解决：

1. **算法能力单一且被语义绑架**：只实现 PID，且写成了"位置专属"（position_pd）。实际上 PID 就是 PID——输入误差、输出控制量，它**不需要知道自己在控制什么**。位置/速度/转向这类语义应当属于被控对象模型，而非控制算法。
2. **控制职责与仿真职责混杂**：`JointSimulator` 把「控制算法（PID）」与「被控对象模型（二重积分 1/s²）」绑定在一个类里，运动学仿真环节被强行放在控制层，与控制层的编排职责纠缠。

**阶段 A 成果**：剥离出 `ylr1d_algorithm_sim`（纯 C++ 库，零 rclcpp）与 `ylr1d_control`（改名，进程内调用算法层），对外接口不变。详见两包 README。

### 0.2 阶段 B 面对的新问题（本阶段主题）

阶段 A 把两层解耦了大半，但**层间接口仍是紧耦合**，暴露了更深层的问题：

3. **层间接口僵硬**：算法层虽已是独立包，但实际上只是控制层的"子包"——控制层进程内**直接调用算法层类**（`pid.compute` / `plant.update`），接口是 C++ 函数签名。算法有任何调整（换控制器、换被控对象、调参逻辑），控制层代码就必须同步修改并重新编译。两层是"调用链"关系，不是独立运行时实体。
4. **采样 / 计算时序耦合**：控制层（负责**采样**）与算法层（负责**计算**）被强行绑在同一个控制循环里，每周期"控制层调算法层 → 等结果 → 继续"串行执行。实际上二者在时序上基本独立：采样是周期性的数据获取，计算是周期性的数据处理，**完全可以通过异步通信解耦**，各按固定频率刷新。
5. **参数集中**：全部控制器（位置 26 + 轮子 4）的 PID 参数集中声明在控制层少数节点上，由控制层统一 declare / 维护。控制器多了控制层节点参数爆炸；单个控制器的调参也要改控制层。
6. **算法层"借壳"ROS2**：算法层虽零 rclcpp 依赖，但以 ament 包形式存在、借用 colcon 装 .so、被控制层以 CMake target 链接——为迁就 ROS2 工作区构建而插入了大量 ROS2 相关特征。这种独立性是"被包裹"的，不是自立的。

**阶段 B 方向**：把算法层从"被调用的插件库"升级为**自治的 ROS2 组件集合**——控制器组件（单关节 / 多关节）+ 仿真被控对象组件（位置 / 速度 / 直通），经 **composition** 合成单进程降低通信开销；每个控制器**自管参数**；控制层退化为**通信节点 + 采样保持器 + 算法层外部接口**，只发布信息，算法层按需选择订阅。

> **本阶段范围：只实现 PID。** 其余算法（MPC、自适应等）仅保留接口预留备注，不作为实施内容。Eigen 作为数学底座引入（见 §4.6），服务于未来动力学/矩阵算法。

---

## 1. 核心概念澄清

### 1.1 控制算法是通用的，不知道控制什么

- PID 输入误差、输出有限幅的控制量 `u`，不关心 `u` 是加速度、位置还是速度。
- **谁解释 `u` 的语义，谁就需要知道控制对象**：被控对象模型把 `u` 解释为加速度；控制层在向物理层发布前按下层接口维护输出语义。
- 有模型控制（未来 MPC / 自适应）需要的预测/参考模型，是控制律内部的**设计假设**，与被控对象模型（真实模型）**不等价**。
- 无模型控制（PID）不需要任何模型。

### 1.2 被控对象模型 = 真实模型

描述被控对象自身的物理动态。此处为关节的二重积分（1/s²，位置）或单积分（1/s，速度）+ 运动学限幅。它是"对象是什么样"的真值，不是给控制算法用的预测模型。

### 1.3 直通（bypass）

不需要仿真环节时（Gazebo / 真机在环），被控对象模型直接透传输入：

```cpp
double update(double control_input, double dt) { return control_input; }
```

阶段 B 中，直通是 plant 组件的一个**配置开关**（`*PlantParams::bypass`），plant 组件三类形态之一。

### 1.4 通信驱动解耦（新增）

控制层与算法层之间**不用 C++ 函数签名，而用 topic 通信**：

- 控制层**发布**相关信息（期望、反馈、指令），算法层组件**按需订阅**自己需要的部分；
- 只改算法（换控制器 / 调参数 / 换被控对象模型）时，**控制层零改动**，只动 launch 里加载的组件与参数；
- 输入只需要发 topic 由控制器接收，输出只需要订阅被控对象，**都保持固定频率刷新**，不要求一一对应的同步调用。

这是阶段 B 解耦的核心机制：把"函数调用链"变成"消息流"，两层从运行时解耦。

### 1.5 固定频率刷新（新增）

- 采样（控制层）与计算（算法层）各按**自己的固定频率**运行，互不阻塞；
- 控制层以采样频率刷新采样保持，算法层控制器/被控对象以控制频率刷新；
- 输入（期望/反馈）只发 topic，控制器按固定频率取最新值计算；输出（控制量/物理量）只发 topic，订阅方按固定频率取最新值。**保持固定频率刷新优于"事件驱动"**：时序确定、可预测、无积压。

### 1.6 关节分类（新增）

控制器组件另外起到**关节分类**作用：先一步把控制上**完全无关**的关节分开，各自成组：

- 位置关节（26，`kPositionLimits`）——双臂 / 升降等，各自独立 PID 控制；
- 速度关节（轮子 4，`kVelocityLimits`）——底盘轮速，独立成组；
- 直通关节（预留）——不需要软仿真的关节走 bypass。

分类后，每个组是一个多关节控制器组件（内部仍逐关节独立 PID），组与组之间话题 / 参数 / 生命周期完全隔离，互不影响。

---

## 2. 目标架构（阶段 B：算法层 ROS2 化）

```
   ┌────────────────────────────────────────────┐
   │  上层：HMI / translate / 外部监测控制        │
   └───────────────┬────────────────────────────┘
                   │ /desired_joint_states、param/service（经控制层）
                   ▼
   ┌─────────────────────────────────────────────────────────────┐
   │  控制层  ylr1d_control（通信节点 + 采样保持器 + 外部接口）    │
   │  · 订阅 /desired_joint_states → 采样保持                     │
   │  · 发布 期望 / 反馈（按组，算法层按需订阅）                   │
   │  · 订阅 被控对象输出 → 采样保持 → 发布 5 组命令（Gazebo）     │
   │  · 对外暴露算法层监测控制接口（param/service 转发）           │
   └──────┬───────────────────────────────┬──────────────────────┘
          │ 期望/反馈 topic（采样保持后）   │ 控制量 u topic
          ▼                                │
   ┌───────────────────────────┐           │
   │  算法层（composition 单进程）│          │
   │  ┌─────────────────────┐  │           │
   │  │ controller 组件      │  │           │
   │  │  · 单关节控制器(模板) │──┼───────────┘
   │  │  · 多关节控制器(模板) │  │
   │  └─────────────────────┘  │
   │  ┌─────────────────────┐  │ 物理量 topic
   │  │ plant 组件           │  │
   │  │  · 位置（1/s²）       │◀─┼────────── 订阅 u
   │  │  · 速度（1/s）        │  │
   │  │  · 直通（bypass）     │──┼────────── 发布物理量
   │  └─────────────────────┘  │
   └───────────────────────────┘
```

- **算法层** = 一组 **ROS2 组件**（`rclcpp_components`），由 composition launch 合成单进程，**内部不直接暴露给上层**；
- **控制层** = 数据枢纽：负责采样、保持、转发、与外部（HMI/translate）对接，**不参与算法计算**；
- 依赖方向：控制层 → 算法层（单向，仅装配/通信）；算法层自治，控制层只发信息。

---

## 3. 数据流与时序

### 3.1 话题约定

| 数据 | 话题 | 发布方 | 订阅方 | 内容 |
|------|------|--------|--------|------|
| 上层期望 | `/desired_joint_states` | translate/HMI | 控制层 | 关节目标位置/速度 |
| 组期望 | `/ctrl/<组>/desired` | 控制层（采样保持） | controller 组件 | 该组关节期望 |
| 组反馈 | `/ctrl/<组>/feedback` | 控制层（采样保持） | controller 组件 | 该组关节反馈（物理层或 plant 输出） |
| 控制量 | `/ctrl/<组>/u` | controller 组件 | plant 组件 | 逐关节控制量 u（加速度语义） |
| 物理量 | `/ctrl/<组>/plant_state` | plant 组件 | 控制层 | 逐关节仿真位置/速度（软仿真模式） |
| 下发命令 | 5 组 commands（`ForwardCommandController`） | 控制层 | 物理层 | 物理层命令 |

> 话题按组命名空间隔离（`/ctrl/chassis/*`、`/ctrl/arm_left/*` …），体现 1.6 关节分类。

### 3.2 时序（采样与计算解耦）

```
控制层（采样频率 fs，如 100Hz）：
  ① 订阅 /desired_joint_states，缓存最新期望
  ② 订阅物理层 /joint_states（直通）或 plant 输出（软仿真），缓存最新反馈
  ③ 固定频率发布 组期望 / 组反馈（采样保持器语义：无新数据时用上一次缓存值）

controller 组件（控制频率 fc，如 100Hz）：
  ④ 订阅最新组期望 / 组反馈
  ⑤ 逐关节 pid.compute → 控制量 u
  ⑥ 固定频率发布 /ctrl/<组>/u

plant 组件（仿真频率 fp，如 100Hz）：
  ⑦ 订阅最新 u
  ⑧ 逐关节 plant.update → 物理量（或 bypass 直通）
  ⑨ 固定频率发布 /ctrl/<组>/plant_state

控制层：
  ⑩ 订阅 plant_state（软仿真）→ 采样保持 → 发布 5 组命令
```

- ①② 与 ④⑤ 之间、⑦⑧ 与 ⑩ 之间**无同步调用**，靠采样保持 + 固定频率取最新值衔接；
- 各频率可独立配置；软仿真时反馈来源是 plant 输出，直通时是物理层 `/joint_states`，由控制层按模式切换转发。

---

## 4. 算法层设计（controller / plant 组件）

算法层从"纯库"演进为"**纯 C++ 算法核心 + ROS2 组件壳**"两层：

- **算法核心**（保持零 rclcpp）：`PidController`、`PositionPlant`、`VelocityPlant` 保留在 `ylr1d_algorithm_sim` 纯库中，可独立单元测试；
- **组件壳**（ROS2 适配）：新建 `ylr1d_controller` / `ylr1d_plant`（或单包 `ylr1d_algorithm_ros`）包裹核心类，负责话题订阅/发布、参数声明、频率刷新。

### 4.1 纯 C++ 算法核心（保留）

沿用阶段 A 资产，接口不变：

- `controller/pid/pid.hpp`：`PidController`（`configure` / `initialize` / `compute` / `reset`）；
- `plant/position/position_plant.hpp`：`PositionPlant`（1/s²，加速度→位置，带位置限位）；
- `plant/velocity/velocity_plant.hpp`：`VelocityPlant`（1/s，加速度→速度，带速度限幅）。

**不设统一抽象基类**（阶段 A 已确认）：各算法各自接口、各自数据格式；方法名风格统一约定，不做类型层面统一。

### 4.2 控制器组件（模板化，单关节 / 多关节）

**模板化单关节控制器**——用模板指定"控制器类 + 被控对象 + 装配参数"：

```cpp
// 核心（纯 C++）：单关节控制逻辑，可在单元测试中直接例化
template <typename ControllerT, typename PlantT, typename ParamsT>
class JointControlCore {
  // ControllerT: 如 PidController；PlantT: 如 PositionPlant / VelocityPlant
  // ParamsT: 如 PositionJointParams / VelocityJointParams（含 pid + plant 参数）
  void configure(const ParamsT & params);
  void initialize(...);
  double compute(double desired, double feedback, double dt);  // 先 pid 后 plant
};

// 组件壳（ROS2）：订阅 desired/feedback，固定频率调用 core，发布 u
template <typename ControllerT, typename PlantT, typename ParamsT>
class JointControllerComponent : public rclcpp::Node {
  JointControlCore<ControllerT, PlantT, ParamsT> core_;
  rclcpp::TimerBase::SharedPtr timer_;   // 固定频率刷新
  // 参数自管：declare_parameter("<关节>/pid/*")、限位来自配置
};
```

例化示例：`JointControllerComponent<PidController, PositionPlant, PositionJointParams>`（位置关节）、`JointControllerComponent<PidController, VelocityPlant, VelocityJointParams>`（轮子）。

**多关节控制器**——组装一组单关节 core，做组级管理：

```cpp
template <typename ControllerT, typename PlantT, typename ParamsT>
class JointGroupControllerComponent : public rclcpp::Node {
  std::vector<JointControlCore<ControllerT, PlantT, ParamsT>> cores_;  // 逐关节
  // 当前逐关节独立控制；未来协同（动力学补偿等）在此扩展
};
```

> 关节组通常做多关节协同，但本阶段**简单采用各自独立控制**。协同点（如"先求加速度 → 动力学补偿 → 滤波 → 发送"）预留扩展位：滤波属控制器行为，补偿属组行为，分层时须区分。

### 4.3 被控对象组件（三类：位置 / 速度 / 直通）

```cpp
class PlantComponent : public rclcpp::Node {
  // 类型由参数/模板指定：PositionPlant（1/s²）/ VelocityPlant（1/s）
  // bypass=true → 直通（update 直接 return u）
  // 订阅 /ctrl/<组>/u，固定频率推进，发布 /ctrl/<组>/plant_state
};
```

- 位置型：订阅 u（加速度语义）→ 逐关节 `PositionPlant::update` → 发布位置；
- 速度型：订阅 u → `VelocityPlant::update` → 发布速度；
- 直通型：`bypass` 置位，`update` 直接透传 u，物理层充当真实被控对象。

### 4.4 参数去中心化（新增）

- 每个 controller 组件实例**自己声明自己的参数**（`<关节>/pid/kp…`），不再集中到控制层节点；
- 装配来源：launch 把 `pid.yaml` 按命名空间注入到各组件实例；限位等物理参数随组件配置；
- `joint_config.hpp`（当前在 `ylr1d_control/include/.../config/`）中的**限位表与关节分组常量迁移到算法层组件包**，使组件不反向依赖控制层；控制层仅引用分组名做话题编排。

### 4.5 Composition

- 组件经 `rclcpp_components` + `ament_index` 注册，launch 用 `composable_node_container` 合成**单进程**，组件间 topic 通信走进程内共享内存，开销接近进程内调用；
- 容器可按组拆分（如底盘容器 + 双臂容器），便于独立启停与故障隔离；
- 多实例用命名空间区分（`/ctrl/chassis/*`、`/ctrl/arm_left/*`）。

### 4.6 ThirdParty Eigen（数学底座）

为未来矩阵/动力学算法（MPC、动力学补偿、任务空间控制）引入 **Eigen**：

- **vendor 方式**：`<WS_ROOT>/ThirdParty/Eigen` 放置 header-only Eigen 源码，算法层 CMake `target_include_directories` 加入该路径，`ament` 打包时不导出、仅构建期使用；
- 当前 PID / 积分器是标量运算，**不依赖 Eigen**；引入仅作基建预留，不强行改造现有代码；
- 后续用到 Eigen 的算法（动力学/矩阵）只依赖算法核心层，组件壳不直接依赖。

---

## 5. 控制层设计（通信节点 + 采样保持器）

由 `ylr1d_control_sim` 改名而来。对外接口不变（输入 `/desired_joint_states`、输出 5 组命令），内部**不再做算法计算**，退化为数据枢纽。

### 5.1 职责

- **采样保持器**：缓存上层期望与物理层/plant 反馈，无新数据时用缓存值，固定频率发布组期望/组反馈；
- **通信节点**：算法层各组件之间的话题转发、组命名空间编排（对应 `joint_config.hpp` 分组常量）；
- **外部接口**：算法层不直接暴露给 HMI / translate，**对外交流一律经控制层**——算法层的监测（状态/诊断）与控制（param/service）由控制层转发，保证单点出入口；
- 看门狗、日志、生命周期（沿用）。

### 5.2 反馈来源与模式切换

- **软仿真**：反馈来自 plant 组件的 `/ctrl/<组>/plant_state`，闭环在算法层进程内完成，可脱离 Gazebo；
- **直通**：反馈来自物理层 `/joint_states`（`joint_state_filter` 已处理 prismatic NaN → 0.0）；
- 由 launch 参数/配置切换，控制层按模式选择转发来源。

### 5.3 初始化时序

- 节点启动后等待首个反馈（软仿真为 plant 首帧，直通为 `/joint_states` 首帧）；
- 随后控制层开始发布采样保持，controller 组件在收到首帧后 `initialize` 各 core；
- 初始化前各组件空转不发布有效数据。

### 5.4 异常处理

- 组件内算法异常由组件捕获上报（诊断话题），控制层做安全行为 + 日志；
- 恢复与降级（如某组失联 → 该组命令保持/归零）由控制层统一编排。

---

## 6. 迁移计划

1. **算法核心保留**：`ylr1d_algorithm_sim` 纯库不动（`PidController` / `PositionPlant` / `VelocityPlant`），继续提供核心与单元测试。
2. **新建组件包**：`ylr1d_algorithm_ros`（或分 `ylr1d_controller` / `ylr1d_plant`）——模板化 `JointControlCore` / `JointControllerComponent` / `JointGroupControllerComponent` / `PlantComponent`，`rclcpp_components` 注册，自管参数。
3. **限位表迁移**：`joint_config.hpp` 的限位/分组常量迁入组件包，控制层只留分组名编排。
4. **控制层改造**：`ylr1d_control` 移除进程内算法调用，改为采样保持 + 话题转发 + 外部接口。
5. **composition launch**：新建/改造 launch 加载组件容器（底盘、双臂等），`pid.yaml` 按命名空间注入。
6. **双模式落地**：软仿真（plant 组件）/ 直通（bypass）均可运行，launch 参数切换。
7. **同步依赖方**：`ylr1d_hmi` / `ylr1d_translate` / `ylr1d_bringup` / `ylr1d_test` 中话题/参数引用同步。
8. **清理旧代码**：确认回归后移除控制层内旧算法调用路径。

> 迁移期间控制层对外话题保持不变，上层无感知。

---

## 7. 测试与验收

| 层 | 测试 | 工具 |
|----|------|------|
| 算法核心 | `PidController` / `PositionPlant` / `VelocityPlant` 单元测试（纯 C++，零 ROS2） | `ylr1d_algorithm_sim/test/`（gtest，已通过） |
| 组件 | 模板例化（位置/速度/直通）组件冒烟：topic 收发、参数加载、频率刷新 | `ylr1d_test` Tier1 |
| 控制层 | 采样保持 + 转发回归，软仿真闭环（PID+plant 单位负反馈） | `ylr1d_test` Tier2 |
| 集成 | composition 全流程：期望 → 组件计算 → plant 仿真 → 命令 → Gazebo | `ylr1d_test` Tier2 |

- `controller_test`（python+qt5+csv）按计划暂缓，需要联调时再恢复。

---

## 8. 分阶段推进

| 阶段 | 内容 | 验收 |
|------|------|------|
| 0 | 现状梳理（本文件 0.1/0.2） | 问题清单确认 |
| 1 | 剥离算法层 + 控制层改名（阶段 A，已完成） | `ylr1d_test` 全量回归通过 |
| 2 | 算法核心拆分：pid 通用化 + Position/VelocityPlant + 闭环测试（已完成） | gtest 通过 |
| 3 | 组件壳 + composition 骨架：模板化控制器组件 / plant 组件（位置/速度/直通）（已完成） | 组件冒烟通过 |
| 4 | 控制层退化为采样保持器 + 通信节点（已完成） | 软仿真闭环回归通过 |
| 5 | 限位表迁移、参数去中心化、双模式切换（已完成） | 软仿真/直通均可运行 |
| 6 | 集成与清理：依赖方同步、旧代码移除（已完成） | 全量回归通过 |

> **实现偏差（阶段 A 记录）**：计划"每算法各自 .so"，实际第一阶段为单 .so（`libylr1d_algorithm_sim.so`），符合第一阶段单包；后续算法增多再拆分。阶段 B 组件化后，组件与核心分包可再评估拆分。

> **实现偏差（阶段 B 记录，2026-08-06）**：
> - 计划「controller 组件 + plant 组件分离 + 中间话题 u / plant_state」，实际**每个"仿真控制器"合成为一个节点**（`SimControllerNode<GroupPlantT>` 模板，controller + plant 都在节点内），层间话题简化为 `/ctrl/<组>/{desired, feedback, output}`，无中间话题。这是对用户"每个仿真控制器是一个节点，固定线程框架 输入→协同→独立→仿真→输出"指令的直接落实。
> - 计划 `JointControlCore` 逐关节 core + `JointGroupControllerComponent` 组级管理，实际核心直接复用标量 `PidController`（逐关节）+ 整体 `PositionGroupPlant` / `VelocityGroupPlant`（Eigen 向量），协同用 `ProportionalController`（P=1）。
> - 计划 Eigen「仅作基建预留、当前不依赖」，实际协同控制器与仿真对象**立即使用** Eigen（`Eigen::VectorXd`），符合用户"现在就 vendor 引入使用"。
> - 直通（bypass）参数已支持（launch 切换留待后续，与阶段 A 一致）。

---

## 9. 决策记录

**已确认（阶段 A）**：

- 只实现 PID；PID 通用化（不绑定 position），语义由外层解释
- 控制层与算法层解耦；不设统一抽象接口（各算法各自接口）
- 算法核心为纯 C++ 库，可独立单元测试
- 逐关节 PID 实例；反馈缓存最新值；等待首帧初始化
- 测试以纯 C++ gtest 为主

**本阶段确认（阶段 B）**：

- 算法层 **ROS2 化**：每个"仿真控制器"是一个 ROS2 节点（`SimControllerNode<GroupPlantT>` 模板），内部固定线程框架 `输入 → 协同(P=1) → 独立(PID) → 仿真对象(整体 Eigen) → 输出`，rclcpp_components 注册，composition 合成单进程
- 层间用 **topic 通信驱动**（`/ctrl/<组>/{desired,feedback,output}`），固定频率刷新；只改算法不动控制层
- 控制层退化为**采样保持器 + 通信节点**，不做算法计算；对外接口不变（`/desired_joint_states` → 5 组命令）
- 协同控制器 = **P=1 比例控制器**（`ProportionalController`，当前透传，未来协同扩展位）；独立控制器 = **PID**（参数与 pid.yaml 相同，逐关节）；仿真对象 = **整体输入/整体输出**（`PositionGroupPlant` / `VelocityGroupPlant`，Eigen 向量化，内部复用标量 plant）
- 控制器节点承担**关节分类**（按控制层分类后的组，每组一节点）
- 参数**移交算法层**：pid.yaml 迁入算法层、按命名空间注入组件；限位表与分组（`joint_config.hpp`）迁入算法层并成为单一来源，控制层 include 引用
- **composition** 合成单进程降低开销
- 引入 **ThirdParty Eigen**（vendor header-only）并**实际使用**（协同与仿真对象均为 Eigen 向量）

**暂缓 / 待定**：

- 算法层 HMI 与"外部对控制器的操作接口"（param/service）的具体协议——先经控制层转发，细节后续定
- 参数热更新触发时机（HMI）——节点构造时读取一次，运行中 `ros2 param set` 不生效
- 多关节协同（动力学补偿等）具体算法——当前 P=1 透传，预留扩展位
- 直通模式（bypass）launch 切换——参数已支持，与阶段 A 一致留待后续
